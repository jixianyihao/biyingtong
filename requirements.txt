flask==3.1.0
flask-socketio==5.4.1
flask-cors==5.0.1
pandas==2.2.3
gevent==24.11.1
gevent-websocket==0.10.1

# vnpy backtest stack (added in P0)
# Deviations from spec pins:
#   - spec asked vnpy>=3.9.0,<4 but vnpy_portfoliostrategy>=1.2.0,
#     vnpy_sqlite>=1.0.7 (only 1.1.x actually exists) and
#     vnpy_ctastrategy>=1.2.1 (only 1.2.0 / 1.3.x / 1.4.x exist)
#     all require vnpy>=4.0.0. Pinned to 4.x accordingly.
#   - spec asked vnpy_tdx>=1.0.6; PyPI only has 1.0.0 and it ships an empty
#     stub (dist-info only, no importable module). Kept the install as
#     documentation; real module must come from the GitHub source or be
#     replaced by the existing tqcenter/TDX SDK wrapper in tdx_service.py.
vnpy>=4.0.0
vnpy_sqlite>=1.1.0
vnpy_tdx>=1.0.0
vnpy_portfoliostrategy>=1.2.0
vnpy_ctastrategy>=1.4.0

# testing (added in P0)
pytest>=7.4.0
pytest-xdist>=3.3.0
