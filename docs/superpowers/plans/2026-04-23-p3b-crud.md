# P3-B Agent + Persona CRUD Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to execute this plan task-by-task.

**Goal:** 让用户能完整管理 agent/persona 生命周期：编辑 agent、删 agent、自建 persona、改 persona（prompt 自动 bump version）、删 persona（被依赖时拒绝）、回滚 agent prompt 到历史版本。

**Architecture:**
- 后端：扩 `AgentStore` + `PersonaStore` Protocol（加 `update` / `delete`），加 `PromptVersionStore.rollback`。5 个新 route：PUT/DELETE agents、POST/PUT/DELETE personas、POST agents/:id/prompts/rollback。内置 persona（is_builtin=True）拒绝 PUT / DELETE。persona PUT 修改 system_prompt 时对所有引用 agent 自动 bump 新 PromptVersion。persona DELETE 时若有 agent 引用则 409。
- 前端：Agent.tsx 加编辑/删除按钮 + Modal；Persona 新增 Persona.tsx 页或复用 Agent 页的 Persona 区域；PromptHistory.tsx 加"回滚到此版本"按钮。

**Tech Stack:** Python 3.10 / Flask / SQLite / React 19 / TypeScript / TanStack Query

---

## File Structure

**Backend:**
- `storage/base.py` — Protocol 扩展（AgentStore + PersonaStore + PromptVersionStore）
- `storage/sqlite_agents.py` — `update`、`delete` 方法
- `storage/sqlite_personas.py` — `delete` 方法（upsert 已有）
- `storage/sqlite_prompt_versions.py` — `rollback` 方法
- `api/agents.py` — PUT /:id、DELETE /:id、POST /:id/prompts/rollback 三路由
- `api/personas.py` — POST、PUT /:id、DELETE /:id 三路由
- `tests/test_p3b_crud.py` — 新文件，按 CRUD 操作分组测试

**Frontend:**
- `frontend/src/api/client.ts` — 6 新 api 方法
- `frontend/src/api/hooks.ts` — 6 新 mutation hooks
- `frontend/src/components/AgentEditModal.tsx` — 新组件
- `frontend/src/components/AgentDeleteDialog.tsx` — 新组件
- `frontend/src/components/PersonaFormModal.tsx` — 新组件（create + edit 复用）
- `frontend/src/pages/Agent.tsx` — agent 编辑/删除 按钮 + Modal 集成；Persona 管理区域
- `frontend/src/pages/PromptHistory.tsx` — 回滚按钮

---

### Task 1: Protocol 扩展 + schema 不变动确认

**Files:**
- Modify: `storage/base.py`
- Test: `tests/test_p3b_crud.py` (new)

**Design notes:**
- `Agent.update(agent_id, *, display_name=None, rules_override=None)` — 只允许改这两个字段。None 表示不改。
- `Agent.delete(agent_id)` — 级联删除 prompt_versions（FK？— 查 schema）+ 该 agent 下所有 backtest_results（这是爆炸半径，要慎重——更稳妥是拒绝删除存在 backtest 的 agent，或者 soft-delete 改 status='deleted'）。**MVP 方案：hard delete agent + prompt_versions，但 backtest_results 保留（session_id 仍可查），改 agents 表 row 前强制用户确认**。实际上先做最简：直接 DELETE FROM agents + prompt_versions，backtest_results 孤立保留（它们的 agent_id 外键软约束）。
- `Persona.delete(persona_id)` — 若有 agent 引用则 raise；否则删行。
- `PromptVersion.rollback(agent_id, version_id)` — 读 version 的 system_prompt，新建一个 version（version_number = max+1）copy 同样的 system_prompt，note=`"rolled back to v<N>"`，返回新版本 + update agents.current_prompt_version_id。

- [ ] **Step 1: 写失败测试**

Create `tests/test_p3b_crud.py`:

```python
"""P3-B CRUD: agent/persona update + delete + prompt rollback."""
from __future__ import annotations

import pytest


def test_agent_store_has_update_method(observability_storage):
    """Protocol exposes `update` on AgentStore."""
    import storage
    store = storage.agents()
    assert hasattr(store, 'update')
    assert callable(store.update)


def test_agent_store_has_delete_method(observability_storage):
    import storage
    store = storage.agents()
    assert hasattr(store, 'delete')
    assert callable(store.delete)


def test_persona_store_has_delete_method(observability_storage):
    import storage
    store = storage.personas()
    assert hasattr(store, 'delete')
    assert callable(store.delete)


def test_prompt_version_store_has_rollback_method(observability_storage):
    import storage
    store = storage.prompt_versions()
    assert hasattr(store, 'rollback')
    assert callable(store.rollback)
```

- [ ] **Step 2: 跑确认失败**

Run: `python -m pytest tests/test_p3b_crud.py -v`
Expected: 4 FAIL.

- [ ] **Step 3: 扩 Protocol 签名 in storage/base.py**

找到 `class AgentStore(Protocol):` 块，追加：

```python
    def update(self, agent_id: str, *,
               display_name: str | None = None,
               rules_override: dict | None = None) -> None:
        """Partial update. None means don't change. Noop if agent missing."""
        ...

    def delete(self, agent_id: str) -> bool:
        """Hard delete the agent + its prompt_versions. Returns True if
        a row was removed. backtest_results for this agent are preserved
        (they reference agent_id by value, not FK)."""
        ...
```

`class PersonaStore(Protocol):`  追加：

```python
    def delete(self, persona_id: str) -> bool:
        """Delete by id. Caller must check dependent agents first — store
        does NOT enforce that constraint."""
        ...
```

`class PromptVersionStore(Protocol):` 追加：

```python
    def rollback(self, agent_id: str, version_id: int) -> PromptVersion:
        """Copy an older version's prompt to a NEW version at max+1.
        Returns the fresh PromptVersion. Does NOT mutate agents table —
        caller (API layer) updates current_prompt_version_id."""
        ...
```

- [ ] **Step 4: 跑测试 — 应通过（Protocol has attr / callable via @runtime_checkable)**

Run: `python -m pytest tests/test_p3b_crud.py -v -k "has_"`
Expected: 4 passed.

- [ ] **Step 5: commit**

```bash
git add storage/base.py tests/test_p3b_crud.py
git commit -m "feat(p3b): extend Protocol with update/delete/rollback signatures"
```

---

### Task 2: SQLiteAgentStore — update + delete

**Files:**
- Modify: `storage/sqlite_agents.py`
- Test: `tests/test_p3b_crud.py`

- [ ] **Step 1: 写 update 测试**

追加到 `tests/test_p3b_crud.py`:

```python
def test_agent_update_display_name(observability_storage):
    import storage
    agent = storage.agents().create_from_persona(
        persona_id='linyuan', model_id='claude-opus-4-7',
        display_name='original', initial_capital=1_000_000.0,
    )
    storage.agents().update(agent.id, display_name='renamed')
    fetched = storage.agents().get(agent.id)
    assert fetched.display_name == 'renamed'
    # rules_override not passed → unchanged
    assert fetched.rules_override == {}


def test_agent_update_rules_override(observability_storage):
    import storage
    agent = storage.agents().create_from_persona(
        persona_id='linyuan', model_id='claude-opus-4-7',
        display_name='t', initial_capital=1_000_000.0,
    )
    storage.agents().update(agent.id, rules_override={'max_holdings': 3})
    fetched = storage.agents().get(agent.id)
    assert fetched.rules_override == {'max_holdings': 3}
    # display_name unchanged
    assert fetched.display_name == 't'


def test_agent_update_both_fields(observability_storage):
    import storage
    agent = storage.agents().create_from_persona(
        persona_id='linyuan', model_id='claude-opus-4-7',
        display_name='t', initial_capital=1_000_000.0,
    )
    storage.agents().update(
        agent.id, display_name='new', rules_override={'k': 1})
    fetched = storage.agents().get(agent.id)
    assert fetched.display_name == 'new'
    assert fetched.rules_override == {'k': 1}


def test_agent_update_nonexistent_is_noop(observability_storage):
    import storage
    # Must not raise
    storage.agents().update('nonexistent', display_name='x')
    assert storage.agents().get('nonexistent') is None
```

- [ ] **Step 2: 跑确认失败**

Run: `python -m pytest tests/test_p3b_crud.py -v -k "update"`
Expected: all FAIL — no `update` method.

- [ ] **Step 3: 实现 update in SQLiteAgentStore**

加方法到 `storage/sqlite_agents.py`:

```python
    def update(self, agent_id: str, *,
               display_name: str | None = None,
               rules_override: dict | None = None) -> None:
        """Partial update. None means don't change. Noop if agent missing."""
        if display_name is None and rules_override is None:
            return  # nothing to update

        sets: list[str] = []
        vals: list = []
        if display_name is not None:
            sets.append('display_name = ?')
            vals.append(display_name)
        if rules_override is not None:
            sets.append('rules_override = ?')
            vals.append(json.dumps(rules_override, ensure_ascii=False))
        vals.append(agent_id)

        con = sqlite3.connect(self._db_path)
        try:
            con.execute(SCHEMA_AGENTS)
            con.execute(
                f'UPDATE agents SET {", ".join(sets)} WHERE id = ?',
                tuple(vals),
            )
            con.commit()
        finally:
            con.close()
```

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest tests/test_p3b_crud.py -v -k "update"`
Expected: 4 passed.

- [ ] **Step 5: 写 delete 测试**

追加：

```python
def test_agent_delete_removes_row(observability_storage):
    import storage
    agent = storage.agents().create_from_persona(
        persona_id='linyuan', model_id='claude-opus-4-7',
        display_name='t', initial_capital=1_000_000.0,
    )
    assert storage.agents().delete(agent.id) is True
    assert storage.agents().get(agent.id) is None


def test_agent_delete_also_removes_prompt_versions(observability_storage):
    import storage
    agent = storage.agents().create_from_persona(
        persona_id='linyuan', model_id='claude-opus-4-7',
        display_name='t', initial_capital=1_000_000.0,
    )
    # v1 auto-created; add v2
    storage.prompt_versions().insert(
        agent_id=agent.id, system_prompt='new prompt', note='test',
    )
    assert len(storage.prompt_versions().list_for_agent(agent.id)) == 2

    storage.agents().delete(agent.id)
    assert storage.prompt_versions().list_for_agent(agent.id) == []


def test_agent_delete_nonexistent_returns_false(observability_storage):
    import storage
    assert storage.agents().delete('nonexistent') is False
```

- [ ] **Step 6: 跑确认失败**

Run: `python -m pytest tests/test_p3b_crud.py -v -k "delete"`
Expected: FAIL on agent ones.

- [ ] **Step 7: 实现 delete**

```python
    def delete(self, agent_id: str) -> bool:
        """Hard delete agent + its prompt_versions. backtest_results preserved."""
        con = sqlite3.connect(self._db_path)
        try:
            con.execute(SCHEMA_AGENTS)
            cur = con.execute(
                'DELETE FROM agents WHERE id = ?', (agent_id,),
            )
            removed = cur.rowcount > 0
            if removed:
                con.execute(
                    'DELETE FROM prompt_versions WHERE agent_id = ?',
                    (agent_id,),
                )
            con.commit()
        finally:
            con.close()
        return removed
```

- [ ] **Step 8: 跑测试确认通过**

Run: `python -m pytest tests/test_p3b_crud.py -v`
Expected: all Task 2 tests passed (4 update + 3 delete + 4 original = 11).

- [ ] **Step 9: commit**

```bash
git add storage/sqlite_agents.py tests/test_p3b_crud.py
git commit -m "feat(p3b): SQLiteAgentStore.update + delete"
```

---

### Task 3: SQLitePersonaStore — delete

**Files:**
- Modify: `storage/sqlite_personas.py`
- Test: `tests/test_p3b_crud.py`

- [ ] **Step 1: 测试**

```python
def test_persona_delete_removes_row(observability_storage):
    import storage
    from backtest.base import Persona  # wrong — Persona is in storage.base
    from storage.base import Persona
    storage.personas().upsert(Persona(
        id='custom_test', name='Custom Test',
        style_desc='test', system_prompt='you are test',
        default_pool=[], pool_filter=None,
        default_schedule='daily', default_rules={},
        allowed_tools=[], is_builtin=False,
    ))
    assert storage.personas().get('custom_test') is not None
    assert storage.personas().delete('custom_test') is True
    assert storage.personas().get('custom_test') is None


def test_persona_delete_nonexistent_returns_false(observability_storage):
    import storage
    assert storage.personas().delete('nonexistent') is False
```

- [ ] **Step 2: 跑失败**

Run: `python -m pytest tests/test_p3b_crud.py -v -k "persona_delete"`

- [ ] **Step 3: 实现**

Add to `SQLitePersonaStore`:

```python
    def delete(self, persona_id: str) -> bool:
        con = sqlite3.connect(self._db_path)
        try:
            con.execute(SCHEMA_PERSONAS)
            cur = con.execute(
                'DELETE FROM personas WHERE id = ?', (persona_id,),
            )
            con.commit()
            return cur.rowcount > 0
        finally:
            con.close()
```

- [ ] **Step 4: 测试通过 + commit**

```bash
git add storage/sqlite_personas.py tests/test_p3b_crud.py
git commit -m "feat(p3b): SQLitePersonaStore.delete"
```

---

### Task 4: SQLitePromptVersionStore — rollback

**Files:**
- Modify: `storage/sqlite_prompt_versions.py`
- Test: `tests/test_p3b_crud.py`

**Design:** rollback(agent, version_id) reads version's system_prompt, creates a fresh version at max+1 with that prompt, note `rolled back to vN`. Does NOT touch agents table.

- [ ] **Step 1: 测试**

```python
def test_prompt_version_rollback_creates_new_version_with_old_prompt(observability_storage):
    import storage
    agent = storage.agents().create_from_persona(
        persona_id='linyuan', model_id='claude-opus-4-7',
        display_name='t', initial_capital=1_000_000.0,
    )
    pv = storage.prompt_versions()
    # v1 is auto-created; make v2
    v2 = pv.insert(agent_id=agent.id, system_prompt='v2 prompt', note='updated')
    assert v2.version_number == 2

    # Rollback to v1
    versions = pv.list_for_agent(agent.id)
    v1 = versions[0]
    assert v1.version_number == 1
    new_version = pv.rollback(agent_id=agent.id, version_id=v1.id)

    assert new_version.version_number == 3
    assert new_version.system_prompt == v1.system_prompt
    assert 'rolled back to v1' in (new_version.note or '')
    # v1 and v2 unchanged
    assert [v.version_number for v in pv.list_for_agent(agent.id)] == [1, 2, 3]


def test_prompt_version_rollback_unknown_version_raises(observability_storage):
    import storage
    agent = storage.agents().create_from_persona(
        persona_id='linyuan', model_id='claude-opus-4-7',
        display_name='t', initial_capital=1_000_000.0,
    )
    with pytest.raises(ValueError, match='not found'):
        storage.prompt_versions().rollback(
            agent_id=agent.id, version_id=99999)


def test_prompt_version_rollback_wrong_agent_raises(observability_storage):
    import storage
    a1 = storage.agents().create_from_persona(
        persona_id='linyuan', model_id='claude-opus-4-7',
        display_name='a1', initial_capital=1_000_000.0,
    )
    a2 = storage.agents().create_from_persona(
        persona_id='linyuan', model_id='claude-opus-4-7',
        display_name='a2', initial_capital=1_000_000.0,
    )
    a1_v1 = storage.prompt_versions().list_for_agent(a1.id)[0]
    with pytest.raises(ValueError, match='does not belong'):
        storage.prompt_versions().rollback(
            agent_id=a2.id, version_id=a1_v1.id)
```

- [ ] **Step 2: 跑失败**

Run: `python -m pytest tests/test_p3b_crud.py -v -k "rollback"`

- [ ] **Step 3: 实现**

```python
    def rollback(self, agent_id: str, version_id: int) -> PromptVersion:
        con = sqlite3.connect(self._db_path)
        try:
            con.execute(SCHEMA_PROMPT_VERSIONS)
            con.execute(SCHEMA_PROMPT_VERSION_INDEX)
            row = con.execute(
                '''SELECT id, agent_id, version_number, system_prompt,
                          created_at, note
                   FROM prompt_versions WHERE id = ?''',
                (version_id,),
            ).fetchone()
            if row is None:
                raise ValueError(f'version {version_id!r} not found')
            if row[1] != agent_id:
                raise ValueError(
                    f'version {version_id} does not belong to agent {agent_id!r}')
            old_prompt = row[3]
            old_version_number = row[2]
        finally:
            con.close()
        # Reuse insert — it handles the version_number math
        return self.insert(
            agent_id=agent_id,
            system_prompt=old_prompt,
            note=f'rolled back to v{old_version_number}',
        )
```

- [ ] **Step 4: 测试通过 + commit**

```bash
git add storage/sqlite_prompt_versions.py tests/test_p3b_crud.py
git commit -m "feat(p3b): SQLitePromptVersionStore.rollback"
```

---

### Task 5: API — PUT/DELETE /api/agents/:id + POST /api/agents/:id/prompts/rollback

**Files:**
- Modify: `api/agents.py`
- Test: `tests/test_p3b_crud.py`

- [ ] **Step 1: 测试**

追加：

```python
import json as _json

def _client_factory(observability_storage):
    """Replicate the client fixture from test_backtest_observability."""
    from app import app as flask_app  # noqa: import at runtime
    # Actually — need _fresh_app pattern. Reuse whatever smoke test uses.
    from flask import Flask
    from api import api_bp
    app = Flask(__name__)
    app.register_blueprint(api_bp)
    return app.test_client()


@pytest.fixture
def client(observability_storage):
    from flask import Flask
    from api import api_bp
    app = Flask(__name__)
    app.register_blueprint(api_bp)
    app.config['TESTING'] = True
    with app.test_client() as c:
        yield c


def test_put_agent_updates_display_name(observability_storage, client):
    import storage
    agent = storage.agents().create_from_persona(
        persona_id='linyuan', model_id='claude-opus-4-7',
        display_name='original', initial_capital=1_000_000.0,
    )
    resp = client.put(
        f'/api/agents/{agent.id}',
        json={'display_name': 'renamed'},
    )
    assert resp.status_code == 200
    data = resp.get_json()
    assert data['display_name'] == 'renamed'
    # DB state confirmed
    assert storage.agents().get(agent.id).display_name == 'renamed'


def test_put_agent_updates_rules_override(observability_storage, client):
    import storage
    agent = storage.agents().create_from_persona(
        persona_id='linyuan', model_id='claude-opus-4-7',
        display_name='t', initial_capital=1_000_000.0,
    )
    resp = client.put(
        f'/api/agents/{agent.id}',
        json={'rules_override': {'max_holdings': 5}},
    )
    assert resp.status_code == 200
    assert resp.get_json()['rules_override'] == {'max_holdings': 5}


def test_put_agent_404_on_missing(observability_storage, client):
    resp = client.put('/api/agents/nope', json={'display_name': 'x'})
    assert resp.status_code == 404


def test_put_agent_empty_body_is_noop_200(observability_storage, client):
    import storage
    agent = storage.agents().create_from_persona(
        persona_id='linyuan', model_id='claude-opus-4-7',
        display_name='t', initial_capital=1_000_000.0,
    )
    resp = client.put(f'/api/agents/{agent.id}', json={})
    assert resp.status_code == 200
    assert resp.get_json()['display_name'] == 't'


def test_delete_agent_204(observability_storage, client):
    import storage
    agent = storage.agents().create_from_persona(
        persona_id='linyuan', model_id='claude-opus-4-7',
        display_name='t', initial_capital=1_000_000.0,
    )
    resp = client.delete(f'/api/agents/{agent.id}')
    assert resp.status_code == 204
    assert storage.agents().get(agent.id) is None


def test_delete_agent_404_on_missing(observability_storage, client):
    resp = client.delete('/api/agents/nope')
    assert resp.status_code == 404


def test_rollback_prompt_creates_new_version(observability_storage, client):
    import storage
    agent = storage.agents().create_from_persona(
        persona_id='linyuan', model_id='claude-opus-4-7',
        display_name='t', initial_capital=1_000_000.0,
    )
    storage.prompt_versions().insert(
        agent_id=agent.id, system_prompt='v2 new', note='edited',
    )
    v1 = storage.prompt_versions().list_for_agent(agent.id)[0]

    resp = client.post(
        f'/api/agents/{agent.id}/prompts/rollback',
        json={'version_id': v1.id},
    )
    assert resp.status_code == 200
    data = resp.get_json()
    assert data['version_number'] == 3
    # agents.current_prompt_version_id updated to the new row
    fresh = storage.agents().get(agent.id)
    assert fresh.current_prompt_version_id == data['id']


def test_rollback_404_on_missing_agent(observability_storage, client):
    resp = client.post(
        '/api/agents/nope/prompts/rollback', json={'version_id': 1})
    assert resp.status_code == 404


def test_rollback_400_on_missing_version_id(observability_storage, client):
    import storage
    agent = storage.agents().create_from_persona(
        persona_id='linyuan', model_id='claude-opus-4-7',
        display_name='t', initial_capital=1_000_000.0,
    )
    resp = client.post(f'/api/agents/{agent.id}/prompts/rollback', json={})
    assert resp.status_code == 400
```

- [ ] **Step 2: 跑失败**

Run: `python -m pytest tests/test_p3b_crud.py -v -k "put_agent or delete_agent or rollback"`

- [ ] **Step 3: 实现 routes — 加到 `api/agents.py`**

```python
@api_bp.route('/agents/<agent_id>', methods=['PUT'])
def update_agent(agent_id):
    """Partial update: display_name, rules_override. 404 if missing."""
    import storage
    a = storage.agents().get(agent_id)
    if a is None:
        return jsonify({'error': 'not_found'}), 404
    body = request.get_json(silent=True) or {}
    display_name = body.get('display_name')
    rules_override = body.get('rules_override')
    storage.agents().update(
        agent_id,
        display_name=display_name if display_name is not None else None,
        rules_override=rules_override if rules_override is not None else None,
    )
    return jsonify(_agent_to_dict(storage.agents().get(agent_id)))


@api_bp.route('/agents/<agent_id>', methods=['DELETE'])
def delete_agent(agent_id):
    import storage
    if storage.agents().get(agent_id) is None:
        return jsonify({'error': 'not_found'}), 404
    storage.agents().delete(agent_id)
    return '', 204


@api_bp.route('/agents/<agent_id>/prompts/rollback', methods=['POST'])
def rollback_agent_prompt(agent_id):
    """Create new prompt version copying an older one. Updates agents.current_prompt_version_id."""
    import storage
    a = storage.agents().get(agent_id)
    if a is None:
        return jsonify({'error': 'not_found'}), 404
    body = request.get_json(silent=True) or {}
    version_id = body.get('version_id')
    if version_id is None:
        return jsonify({'error': 'version_id required'}), 400
    try:
        new_version = storage.prompt_versions().rollback(
            agent_id=agent_id, version_id=int(version_id))
    except ValueError as e:
        return jsonify({'error': str(e)}), 400
    # Point agent at the new version
    import sqlite3
    from pathlib import Path
    # Use store-private method would be cleaner — for MVP, raw UPDATE
    con = sqlite3.connect(storage.agents()._db_path)  # type: ignore[attr-defined]
    try:
        con.execute(
            'UPDATE agents SET current_prompt_version_id = ? WHERE id = ?',
            (new_version.id, agent_id),
        )
        con.commit()
    finally:
        con.close()
    return jsonify({
        'id': new_version.id,
        'agent_id': new_version.agent_id,
        'version_number': new_version.version_number,
        'system_prompt': new_version.system_prompt,
        'created_at': new_version.created_at,
        'note': new_version.note,
    })
```

Better: add a `AgentStore.set_current_prompt_version(agent_id, version_id)` helper so endpoints don't reach into internals. But minimize scope — the raw sqlite3 path is fine for MVP.

Actually — do the helper. It's 4 more lines and is the right shape. Add to Protocol + impl:

Protocol:
```python
    def set_current_prompt_version(self, agent_id: str, version_id: int) -> None:
        """Point agents.current_prompt_version_id at version_id. Noop if
        agent not found."""
        ...
```

Impl in `SQLiteAgentStore`:
```python
    def set_current_prompt_version(self, agent_id: str, version_id: int) -> None:
        con = sqlite3.connect(self._db_path)
        try:
            con.execute(
                'UPDATE agents SET current_prompt_version_id = ? WHERE id = ?',
                (int(version_id), agent_id),
            )
            con.commit()
        finally:
            con.close()
```

Replace the raw SQL in the endpoint with `storage.agents().set_current_prompt_version(agent_id, new_version.id)`.

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest tests/test_p3b_crud.py -v -k "put_agent or delete_agent or rollback"`
Expected: all pass.

- [ ] **Step 5: regression**

Run: `python -m pytest tests/test_api_p2e.py tests/test_api_p2e_backtests_async.py -v 2>&1 | tail -15`
Expected: all pass.

- [ ] **Step 6: commit**

```bash
git add storage/base.py storage/sqlite_agents.py api/agents.py tests/test_p3b_crud.py
git commit -m "feat(p3b): PUT/DELETE /api/agents/:id + POST /prompts/rollback"
```

---

### Task 6: API — POST/PUT/DELETE /api/personas

**Files:**
- Modify: `api/personas.py`
- Test: `tests/test_p3b_crud.py`

**Design:**
- POST body: `{id, name, style_desc, system_prompt, default_pool?, pool_filter?, default_schedule?, default_rules?, allowed_tools?}`. `is_builtin` forced False. 409 if id already exists.
- PUT body: same fields but all optional. 404 if missing. 403 if persona.is_builtin.
- PUT changing system_prompt: for each agent referencing this persona, auto-insert a new PromptVersion with the new prompt (note: `"persona.{id} system_prompt updated"`), and update agents.current_prompt_version_id.
- DELETE: 404 if missing. 403 if persona.is_builtin. 409 if any agent references this persona.

- [ ] **Step 1: 测试**

```python
def test_post_persona_creates(observability_storage, client):
    import storage
    body = {
        'id': 'custom1',
        'name': 'Custom 1',
        'style_desc': 'my custom style',
        'system_prompt': 'You are a custom agent.',
        'default_schedule': 'daily',
    }
    resp = client.post('/api/personas', json=body)
    assert resp.status_code == 201
    data = resp.get_json()
    assert data['id'] == 'custom1'
    assert data['is_builtin'] is False
    # DB confirmed
    p = storage.personas().get('custom1')
    assert p is not None
    assert p.is_builtin is False


def test_post_persona_409_on_duplicate_id(observability_storage, client):
    resp1 = client.post('/api/personas', json={
        'id': 'linyuan',  # already seeded as builtin
        'name': 'x', 'style_desc': 'x', 'system_prompt': 'x',
    })
    assert resp1.status_code == 409


def test_post_persona_400_on_missing_fields(observability_storage, client):
    resp = client.post('/api/personas', json={'id': 'x'})
    assert resp.status_code == 400


def test_put_persona_updates_non_prompt_field(observability_storage, client):
    import storage
    from storage.base import Persona
    storage.personas().upsert(Persona(
        id='c2', name='C2', style_desc='old', system_prompt='old prompt',
        default_pool=[], pool_filter=None, default_schedule='daily',
        default_rules={}, allowed_tools=[], is_builtin=False,
    ))
    resp = client.put('/api/personas/c2', json={'name': 'C2 NEW'})
    assert resp.status_code == 200
    assert storage.personas().get('c2').name == 'C2 NEW'


def test_put_persona_system_prompt_bumps_agent_versions(observability_storage, client):
    import storage
    from storage.base import Persona
    storage.personas().upsert(Persona(
        id='c3', name='C3', style_desc='v', system_prompt='v1',
        default_pool=[], pool_filter=None, default_schedule='daily',
        default_rules={}, allowed_tools=[], is_builtin=False,
    ))
    agent = storage.agents().create_from_persona(
        persona_id='c3', model_id='claude-opus-4-7',
        display_name='t', initial_capital=1_000_000.0,
    )
    assert len(storage.prompt_versions().list_for_agent(agent.id)) == 1

    resp = client.put(
        '/api/personas/c3', json={'system_prompt': 'v2 improved'})
    assert resp.status_code == 200

    versions = storage.prompt_versions().list_for_agent(agent.id)
    assert len(versions) == 2
    assert versions[1].system_prompt == 'v2 improved'
    assert 'c3' in (versions[1].note or '')
    # agent now points at v2
    fresh = storage.agents().get(agent.id)
    assert fresh.current_prompt_version_id == versions[1].id


def test_put_persona_403_on_builtin(observability_storage, client):
    resp = client.put('/api/personas/linyuan', json={'name': 'NOT ALLOWED'})
    assert resp.status_code == 403


def test_put_persona_404_on_missing(observability_storage, client):
    resp = client.put('/api/personas/nope', json={'name': 'x'})
    assert resp.status_code == 404


def test_delete_persona_204(observability_storage, client):
    import storage
    from storage.base import Persona
    storage.personas().upsert(Persona(
        id='c4', name='c4', style_desc='x', system_prompt='x',
        default_pool=[], pool_filter=None, default_schedule='daily',
        default_rules={}, allowed_tools=[], is_builtin=False,
    ))
    resp = client.delete('/api/personas/c4')
    assert resp.status_code == 204
    assert storage.personas().get('c4') is None


def test_delete_persona_403_on_builtin(observability_storage, client):
    resp = client.delete('/api/personas/linyuan')
    assert resp.status_code == 403


def test_delete_persona_409_when_agent_references_it(observability_storage, client):
    import storage
    from storage.base import Persona
    storage.personas().upsert(Persona(
        id='c5', name='c5', style_desc='x', system_prompt='x',
        default_pool=[], pool_filter=None, default_schedule='daily',
        default_rules={}, allowed_tools=[], is_builtin=False,
    ))
    storage.agents().create_from_persona(
        persona_id='c5', model_id='claude-opus-4-7',
        display_name='t', initial_capital=1_000_000.0,
    )
    resp = client.delete('/api/personas/c5')
    assert resp.status_code == 409
    # Persona still exists
    assert storage.personas().get('c5') is not None


def test_delete_persona_404_on_missing(observability_storage, client):
    resp = client.delete('/api/personas/nope')
    assert resp.status_code == 404
```

- [ ] **Step 2: 跑失败**

Run: `python -m pytest tests/test_p3b_crud.py -v -k "persona"`

- [ ] **Step 3: 实现**

Edit `api/personas.py`:

```python
from flask import jsonify, request

from . import api_bp


def _persona_to_dict(p) -> dict:
    return {
        'id': p.id, 'name': p.name, 'style_desc': p.style_desc,
        'system_prompt': p.system_prompt,
        'default_pool': p.default_pool, 'pool_filter': p.pool_filter,
        'default_schedule': p.default_schedule,
        'default_rules': p.default_rules,
        'allowed_tools': p.allowed_tools,
        'is_builtin': p.is_builtin,
    }


@api_bp.route('/personas', methods=['POST'])
def create_persona():
    import storage
    from storage.base import Persona
    body = request.get_json(silent=True) or {}
    required = ['id', 'name', 'style_desc', 'system_prompt']
    missing = [k for k in required if not body.get(k)]
    if missing:
        return jsonify({'error': f'missing required fields: {missing}'}), 400
    if storage.personas().get(body['id']) is not None:
        return jsonify({'error': f'persona {body["id"]} already exists'}), 409
    persona = Persona(
        id=body['id'], name=body['name'], style_desc=body['style_desc'],
        system_prompt=body['system_prompt'],
        default_pool=body.get('default_pool') or [],
        pool_filter=body.get('pool_filter'),
        default_schedule=body.get('default_schedule') or 'daily',
        default_rules=body.get('default_rules') or {},
        allowed_tools=body.get('allowed_tools') or [],
        is_builtin=False,  # user-created never builtin
    )
    storage.personas().upsert(persona)
    return jsonify(_persona_to_dict(persona)), 201


@api_bp.route('/personas/<persona_id>', methods=['PUT'])
def update_persona(persona_id):
    import storage
    from storage.base import Persona
    p = storage.personas().get(persona_id)
    if p is None:
        return jsonify({'error': 'not_found'}), 404
    if p.is_builtin:
        return jsonify({'error': 'cannot edit builtin persona'}), 403
    body = request.get_json(silent=True) or {}

    # Merge with current
    updated = Persona(
        id=p.id,
        name=body.get('name', p.name),
        style_desc=body.get('style_desc', p.style_desc),
        system_prompt=body.get('system_prompt', p.system_prompt),
        default_pool=body.get('default_pool', p.default_pool),
        pool_filter=body.get('pool_filter', p.pool_filter),
        default_schedule=body.get('default_schedule', p.default_schedule),
        default_rules=body.get('default_rules', p.default_rules),
        allowed_tools=body.get('allowed_tools', p.allowed_tools),
        is_builtin=False,
    )
    storage.personas().upsert(updated)

    # If system_prompt changed, bump prompt_version on every referencing agent
    if updated.system_prompt != p.system_prompt:
        for a in storage.agents().list_all():
            if a.persona_id == persona_id:
                pv = storage.prompt_versions().insert(
                    agent_id=a.id,
                    system_prompt=updated.system_prompt,
                    note=f'persona.{persona_id} system_prompt updated',
                )
                storage.agents().set_current_prompt_version(a.id, pv.id)

    return jsonify(_persona_to_dict(updated))


@api_bp.route('/personas/<persona_id>', methods=['DELETE'])
def delete_persona(persona_id):
    import storage
    p = storage.personas().get(persona_id)
    if p is None:
        return jsonify({'error': 'not_found'}), 404
    if p.is_builtin:
        return jsonify({'error': 'cannot delete builtin persona'}), 403
    # Check for referencing agents
    for a in storage.agents().list_all():
        if a.persona_id == persona_id:
            return jsonify({
                'error': f'persona {persona_id} is referenced by agent {a.id}',
            }), 409
    storage.personas().delete(persona_id)
    return '', 204
```

- [ ] **Step 4: 跑测试 + commit**

Run: `python -m pytest tests/test_p3b_crud.py -v`
Expected: all new tests pass.

Regression: `python -m pytest tests/test_api_p2e.py -v 2>&1 | tail -10`

```bash
git add api/personas.py tests/test_p3b_crud.py
git commit -m "feat(p3b): POST/PUT/DELETE /api/personas"
```

---

### Task 7: Frontend — client + hooks

**Files:**
- Modify: `frontend/src/api/client.ts`
- Modify: `frontend/src/api/hooks.ts`
- Modify: `frontend/src/api/types.ts` (add `CreatePersonaBody` + `UpdateAgentBody` types)

- [ ] **Step 1: 加 types**

In `frontend/src/api/types.ts`:

```typescript
export type UpdateAgentBody = {
  display_name?: string;
  rules_override?: Record<string, unknown>;
};

export type CreatePersonaBody = {
  id: string;
  name: string;
  style_desc: string;
  system_prompt: string;
  default_pool?: string[];
  pool_filter?: Record<string, unknown> | null;
  default_schedule?: string;
  default_rules?: Record<string, unknown>;
  allowed_tools?: string[];
};

export type UpdatePersonaBody = Partial<Omit<CreatePersonaBody, 'id'>>;
```

- [ ] **Step 2: 加 client 方法**

In `frontend/src/api/client.ts`, extend `api` object:

```typescript
  updateAgent: (id: string, body: UpdateAgentBody) =>
    request<Agent>(`/api/agents/${id}`, {
      method: 'PUT', body: JSON.stringify(body),
    }),
  deleteAgent: (id: string) =>
    request<void>(`/api/agents/${id}`, { method: 'DELETE' }),
  rollbackPrompt: (agentId: string, versionId: number) =>
    request<PromptVersion>(
      `/api/agents/${agentId}/prompts/rollback`,
      { method: 'POST', body: JSON.stringify({ version_id: versionId }) },
    ),
  createPersona: (body: CreatePersonaBody) =>
    request<Persona>('/api/personas', {
      method: 'POST', body: JSON.stringify(body),
    }),
  updatePersona: (id: string, body: UpdatePersonaBody) =>
    request<Persona>(`/api/personas/${id}`, {
      method: 'PUT', body: JSON.stringify(body),
    }),
  deletePersona: (id: string) =>
    request<void>(`/api/personas/${id}`, { method: 'DELETE' }),
```

Don't forget to import the new type names at top of client.ts.

`request<void>` may need adjusting if `request` requires a JSON response — check the impl. Likely use `request<null>` or a variant that skips JSON parse on 204.

- [ ] **Step 3: 加 hooks**

In `frontend/src/api/hooks.ts`:

```typescript
export const useUpdateAgent = () => {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: ({ id, body }: { id: string; body: UpdateAgentBody }) =>
      api.updateAgent(id, body),
    onSuccess: (_d, { id }) => {
      qc.invalidateQueries({ queryKey: ['agents'] });
      qc.invalidateQueries({ queryKey: ['agent', id] });
    },
  });
};

export const useDeleteAgent = () => {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: (id: string) => api.deleteAgent(id),
    onSuccess: () => {
      qc.invalidateQueries({ queryKey: ['agents'] });
    },
  });
};

export const useRollbackPrompt = () => {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: ({ agentId, versionId }: { agentId: string; versionId: number }) =>
      api.rollbackPrompt(agentId, versionId),
    onSuccess: (_d, { agentId }) => {
      qc.invalidateQueries({ queryKey: ['agent-prompts', agentId] });
      qc.invalidateQueries({ queryKey: ['agent', agentId] });
    },
  });
};

export const useCreatePersona = () => {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: api.createPersona,
    onSuccess: () => qc.invalidateQueries({ queryKey: ['personas'] }),
  });
};

export const useUpdatePersona = () => {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: ({ id, body }: { id: string; body: UpdatePersonaBody }) =>
      api.updatePersona(id, body),
    onSuccess: () => qc.invalidateQueries({ queryKey: ['personas'] }),
  });
};

export const useDeletePersona = () => {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: (id: string) => api.deletePersona(id),
    onSuccess: () => qc.invalidateQueries({ queryKey: ['personas'] }),
  });
};
```

Add `UpdateAgentBody / UpdatePersonaBody / CreatePersonaBody` to the hooks.ts type imports.

- [ ] **Step 4: build**

Run: `cd frontend && npm run build`
Expected: pass.

- [ ] **Step 5: commit**

```bash
git add frontend/src/api/types.ts frontend/src/api/client.ts frontend/src/api/hooks.ts
git commit -m "feat(p3b): frontend types + client + hooks for CRUD"
```

---

### Task 8: AgentEditModal + AgentDeleteDialog components

**Files:**
- Create: `frontend/src/components/AgentEditModal.tsx`
- Create: `frontend/src/components/AgentDeleteDialog.tsx`

Both are modal dialogs (fixed position overlay). Reuse the same pattern as existing Modals — check `frontend/src/pages/Risk.tsx` for RedLineConfigModal if it exists, or just build vanilla.

AgentEditModal:
- Takes `agent: Agent`, `onClose: () => void`
- Local state: `display_name`, `rules_override_json_text` (textarea for JSON)
- "保存" button calls `useUpdateAgent().mutate({id, body})` — on success, closes modal
- JSON parse error → inline error state, no submit

AgentDeleteDialog:
- Takes `agent: Agent`, `onClose: () => void`, `onConfirmed: () => void`
- Shows confirmation text: "此操作不可撤销。agent 的 backtest 历史会保留但无法再运行。"
- "确认删除" red button → `useDeleteAgent().mutate(agent.id)` + onClose + onConfirmed

Implementation code — use inline styles + Tailwind helpers consistent with other modals.

```typescript
// AgentEditModal.tsx
import { useState } from 'react';
import { useUpdateAgent } from '../api/hooks';
import type { Agent } from '../api/types';

export function AgentEditModal({
  agent,
  onClose,
}: {
  agent: Agent;
  onClose: () => void;
}) {
  const [displayName, setDisplayName] = useState(agent.display_name);
  const [rulesText, setRulesText] = useState(
    JSON.stringify(agent.rules_override, null, 2),
  );
  const [jsonError, setJsonError] = useState<string | null>(null);
  const update = useUpdateAgent();

  const onSave = () => {
    let rulesParsed: Record<string, unknown> | undefined;
    const trimmed = rulesText.trim();
    if (trimmed) {
      try {
        rulesParsed = JSON.parse(trimmed);
      } catch (e) {
        setJsonError(`JSON parse error: ${e instanceof Error ? e.message : String(e)}`);
        return;
      }
    }
    setJsonError(null);
    update.mutate(
      {
        id: agent.id,
        body: {
          display_name: displayName.trim() !== agent.display_name ? displayName.trim() : undefined,
          rules_override: rulesParsed ?? undefined,
        },
      },
      { onSuccess: onClose },
    );
  };

  return (
    <div style={overlayStyle} onClick={onClose}>
      <div
        className="panel"
        style={dialogStyle}
        onClick={(e) => e.stopPropagation()}
      >
        <h2 className="text-text-hi text-base font-semibold mb-3">编辑 Agent</h2>
        <label className="text-[10px] text-text-faint uppercase tracking-wider mb-1 block">
          Display Name
        </label>
        <input
          className="w-full bg-bg-2 border border-panel-border-soft rounded px-3 py-2 text-sm text-text-hi mb-3"
          value={displayName}
          onChange={(e) => setDisplayName(e.target.value)}
        />
        <label className="text-[10px] text-text-faint uppercase tracking-wider mb-1 block">
          Rules Override (JSON)
        </label>
        <textarea
          className="w-full bg-bg-2 border border-panel-border-soft rounded px-3 py-2 text-xs text-text-hi mono mb-2"
          rows={8}
          value={rulesText}
          onChange={(e) => setRulesText(e.target.value)}
        />
        {jsonError && (
          <div className="text-xs mb-3" style={{ color: 'var(--down)' }}>
            {jsonError}
          </div>
        )}
        {update.isError && (
          <div className="text-xs mb-3" style={{ color: 'var(--down)' }}>
            {String(update.error)}
          </div>
        )}
        <div className="flex gap-2 justify-end mt-3">
          <button className="btn" onClick={onClose}>取消</button>
          <button
            className="btn primary"
            onClick={onSave}
            disabled={update.isPending}
          >
            {update.isPending ? '保存中…' : '保存'}
          </button>
        </div>
      </div>
    </div>
  );
}

const overlayStyle: React.CSSProperties = {
  position: 'fixed', inset: 0,
  background: 'rgba(0,0,0,0.6)',
  display: 'flex', alignItems: 'center', justifyContent: 'center',
  zIndex: 1000,
};

const dialogStyle: React.CSSProperties = {
  width: 'min(520px, 90vw)',
  maxHeight: '90vh',
  overflowY: 'auto',
  padding: '20px 24px',
};
```

```typescript
// AgentDeleteDialog.tsx
import { useDeleteAgent } from '../api/hooks';
import type { Agent } from '../api/types';

export function AgentDeleteDialog({
  agent,
  onClose,
  onDeleted,
}: {
  agent: Agent;
  onClose: () => void;
  onDeleted?: () => void;
}) {
  const del = useDeleteAgent();
  const confirm = () => {
    del.mutate(agent.id, {
      onSuccess: () => {
        onClose();
        onDeleted?.();
      },
    });
  };
  return (
    <div style={overlayStyle} onClick={onClose}>
      <div className="panel" style={dialogStyle} onClick={(e) => e.stopPropagation()}>
        <h2 className="text-text-hi text-base font-semibold mb-3">
          删除 Agent
        </h2>
        <div className="text-sm text-text mb-2">
          确认删除 <span className="mono">{agent.display_name}</span>
          <span className="text-text-faint"> ({agent.id})</span> ?
        </div>
        <div className="text-xs text-text-faint mb-4" style={{ lineHeight: 1.5 }}>
          · agent 及其所有 prompt 版本将被永久删除<br/>
          · 历史 backtest 结果会保留（可从 session 查询）<br/>
          · 此操作不可撤销
        </div>
        {del.isError && (
          <div className="text-xs mb-3" style={{ color: 'var(--down)' }}>
            {String(del.error)}
          </div>
        )}
        <div className="flex gap-2 justify-end">
          <button className="btn" onClick={onClose}>取消</button>
          <button
            className="btn"
            onClick={confirm}
            disabled={del.isPending}
            style={{ background: 'var(--down)', color: 'var(--bg)' }}
          >
            {del.isPending ? '删除中…' : '确认删除'}
          </button>
        </div>
      </div>
    </div>
  );
}

const overlayStyle: React.CSSProperties = {
  position: 'fixed', inset: 0, background: 'rgba(0,0,0,0.6)',
  display: 'flex', alignItems: 'center', justifyContent: 'center',
  zIndex: 1000,
};
const dialogStyle: React.CSSProperties = {
  width: 'min(440px, 90vw)', padding: '20px 24px',
};
```

- [ ] **Build check + commit**

```bash
git add frontend/src/components/AgentEditModal.tsx frontend/src/components/AgentDeleteDialog.tsx
git commit -m "feat(p3b): AgentEditModal + AgentDeleteDialog"
```

---

### Task 9: PersonaFormModal (create + edit shared)

**Files:**
- Create: `frontend/src/components/PersonaFormModal.tsx`

One component, two modes — `mode: 'create' | 'edit'` prop. In edit mode, receive existing persona to prefill. On submit, call createPersona or updatePersona hook. Builtin personas don't use this at all (gated in parent).

Scope of form fields: id (create-only, disabled in edit), name, style_desc, system_prompt (textarea), default_schedule, default_pool (comma list), allowed_tools (comma list). pool_filter and default_rules hidden behind "Advanced (JSON)" — keep simple.

Write a clean implementation — the pattern mirrors AgentEditModal.

- [ ] **Build + commit**

---

### Task 10: Agent.tsx — wire edit + delete buttons

**Files:**
- Modify: `frontend/src/pages/Agent.tsx`

Find the agent detail card / row render. Add two icon buttons near the display name: edit (pencil) and delete (trash). Each opens the respective modal. Use existing `useState` for modal open state.

Exact UX: edit button next to `display_name`; delete button in a small overflow menu or separately. User never asked for polished icons — use Unicode `✎` for edit, `🗑` for delete OR just text-only `编辑 / 删除` pills.

In the delete dialog's `onDeleted` callback: if we're on the agent detail page, navigate back to the agents list (`useNavigate()`).

- [ ] **Build + smoke + commit**

---

### Task 11: Persona management section

**Files:**
- Modify: `frontend/src/pages/Agent.tsx` — OR create a new page `frontend/src/pages/Personas.tsx` and add route

**Decision point (present to user if unclear):** put Persona management as:
(a) A new section on the Agent page (under the agent list)
(b) A new top-level page `/personas`

For MVP simplicity, (a) is fine. The Agent page already shows persona options; adding CRUD next to them is natural.

List existing personas (use `usePersonas()` result). For each row:
- Name + style_desc
- "编辑" button if !is_builtin
- "删除" button if !is_builtin
- Builtin rows get a "内置" badge, no buttons

Header has a "新建 Persona" button → opens PersonaFormModal in create mode.

- [ ] **Build + smoke + commit**

---

### Task 12: PromptHistory rollback button

**Files:**
- Modify: `frontend/src/pages/PromptHistory.tsx`

Find the version list rendering. For each version row that's NOT the current one (current version's id != agent.current_prompt_version_id), add a "回滚到此版本" button. On click:
- Inline confirm (小的下拉 or second-tap): "此操作会新建版本 v(max+1)，复制当前版本的 prompt。确定？"
- Then call `useRollbackPrompt().mutate({agentId, versionId})`
- On success: refetch prompt_versions query (hook already invalidates it)

- [ ] **Build + smoke + commit**

---

### Task 13: Full regression + mark roadmap done

- [ ] **Step 1: pytest**

```
python -m pytest -q
```
Expected: 451 + new P3-B tests, 0 regressions.

- [ ] **Step 2: frontend build + lint**

```
cd frontend && npm run build && npm run lint
```
Expected: clean.

- [ ] **Step 3: dev-server UI smoke**

Manual: navigate to `/agent`, edit an agent, delete one, create a persona, edit it, delete it, rollback a prompt version. Take 2-3 screenshots for sanity.

- [ ] **Step 4: update status-and-roadmap**

Edit `docs/superpowers/plans/2026-04-23-status-and-roadmap.md`:
- Mark P3-B section `✅ Done 2026-04-23`
- Append `2026-04-23-p3b-crud.md ✅ Done` to Section 7

- [ ] **Step 5: final commit + merge**

```bash
git add docs/superpowers/plans/2026-04-23-status-and-roadmap.md
git commit -m "docs(p3b): mark CRUD done"
```

Then use `finishing-a-development-branch` skill — options presented, merge to main or PR.

---

## Self-Review

**Spec coverage check:**
- §15.2 `POST /api/personas` (自建) → Task 6 ✅
- §15.2 `PUT /api/personas/{id}` → Task 6 ✅
- §15.2 `DELETE /api/personas/{id}` → Task 6 ✅
- §15.2 `PUT /api/agents/{id}` (rules_override / display_name) → Task 5 ✅
- §15.2 `DELETE /api/agents/{id}` → Task 5 ✅
- §15.3 `POST /api/agents/{id}/prompts/rollback` → Task 5 ✅
- §16 Agent edit/delete buttons → Task 10 ✅
- §16 Persona create form → Task 9+11 ✅
- §16 PromptHistory rollback button → Task 12 ✅

**Missing from spec:**
- `POST /api/agents/{id}/deploy` — 依赖 §13 subprocess (延后)
- `POST /api/agents/{id}/stop` — 同上
- `POST /api/agents/{id}/approve` / reject — 依赖 LiveTrading (延后)

**Type consistency:**
- `UpdateAgentBody` 前后端一致 `{display_name?: string; rules_override?: Record<string,unknown>}`
- `CreatePersonaBody` 前后端一致，`is_builtin` 永远被后端强制 False
- `rollback` response 是 PromptVersion 结构

**Placeholder scan:** 无 TBD；每步都有代码。

---

## Execution Handoff

按 subagent-driven-development 一个任务一个 subagent。review loop 照 P3-A 套路。

单任务预估：
- Task 1: 15 min — Protocol signatures
- Task 2: 30 min — AgentStore update/delete
- Task 3: 15 min — PersonaStore delete
- Task 4: 25 min — PromptVersionStore rollback
- Task 5: 40 min — 3 agent endpoints
- Task 6: 60 min — 3 persona endpoints + system_prompt bump logic
- Task 7: 20 min — client + hooks
- Task 8: 30 min — AgentEdit + AgentDelete modals
- Task 9: 35 min — PersonaFormModal
- Task 10: 25 min — Agent.tsx wiring
- Task 11: 30 min — Persona management section
- Task 12: 25 min — PromptHistory rollback
- Task 13: 20 min — regression + smoke + roadmap

Total: ~6.5 hours + review overhead ≈ 1.5 days，和 roadmap 预估 1.5 天吻合。
